require('dotenv').config();

const {
  PORT,
  DB_URL,
  MQTT_HOST,
  MQTT_PORT,
  MQTT_PROTOCOL,
  MQTT_USERNAME,
  MQTT_PASSWORD,
  MAIL_HOST,
  MAIL_PORT,
  MAIL_USER,
  MAIL_PASSWORD,
  MQTT_TOPIC,
  VERIFICATION_TOKEN_LONG_EXPIRY,
  VERIFICATION_TOKEN_SHORT_EXPIRY,
  REFRESH_TOKEN_SECRET,
  REFRESH_TOKEN_LONG_EXPIRY,
  REFRESH_TOKEN_SHORT_EXPIRY,
  ACCESS_TOKEN_SECRET,
  ACCESS_TOKEN_EXPIRY,
} = process.env;

module.exports = {
  PORT,
  DB_URL,
  MQTT_HOST,
  MQTT_PORT,
  MQTT_PROTOCOL,
  MQTT_USERNAME,
  MQTT_PASSWORD,
  MQTT_TOPIC,
  MAIL_HOST,
  MAIL_PORT,
  MAIL_USER,
  MAIL_PASSWORD,
  VERIFICATION_TOKEN_LONG_EXPIRY,
  VERIFICATION_TOKEN_SHORT_EXPIRY,
  REFRESH_TOKEN_SECRET,
  REFRESH_TOKEN_LONG_EXPIRY,
  REFRESH_TOKEN_SHORT_EXPIRY,
  ACCESS_TOKEN_SECRET,
  ACCESS_TOKEN_EXPIRY,
};
