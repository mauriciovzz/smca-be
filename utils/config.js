require('dotenv').config();

const {
  PORT,
  DB_URL,
  MQTT_HOST,
  MQTT_PORT,
  MQTT_PROTOCOL,
  MQTT_USERNAME,
  MQTT_PASSWORD,
  MAIL_HOST,
  MAIL_PORT,
  MAIL_USER,
  MAIL_PASSWORD,
  MQTT_TOPIC,
  SECRET,
  RT_LONG_EXPIRATION,
  RT_SHORT_EXPIRATION,
  AT_EXPIRATION,
  AV_EXPIRATION,
  EV_EXPIRATION,
  PR_EXPIRATION,
} = process.env;

module.exports = {
  PORT,
  DB_URL,
  MQTT_HOST,
  MQTT_PORT,
  MQTT_PROTOCOL,
  MQTT_USERNAME,
  MQTT_PASSWORD,
  MQTT_TOPIC,
  MAIL_HOST,
  MAIL_PORT,
  MAIL_USER,
  MAIL_PASSWORD,
  SECRET,
  RT_LONG_EXPIRATION,
  RT_SHORT_EXPIRATION,
  AT_EXPIRATION,
  AV_EXPIRATION,
  EV_EXPIRATION,
  PR_EXPIRATION,
};
